# Claude Project Structure

Este repositorio separa responsabilidades por carpeta:

- Backend .NET en `backend/`
- Frontend web en `frontend/`
- Documentación en `docs/`
- Utilidades y scripts en `tools/`

Mantén las decisiones de arquitectura, guías y runbooks en `docs/` para que la raíz quede limpia.
